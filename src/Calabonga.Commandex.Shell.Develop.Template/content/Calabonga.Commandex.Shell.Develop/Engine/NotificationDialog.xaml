<UserControl x:Class="Calabonga.Commandex.Shell.Develop.Engine.NotificationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="500"
             MinHeight="150"
             MaxHeight="400"
             Margin="4">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer
            Grid.Row="0"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            VerticalScrollBarVisibility="Auto">
            <TextBox TextWrapping="Wrap" BorderThickness="0"
                     VerticalScrollBarVisibility="Auto"
                     VerticalAlignment="Stretch"
                     HorizontalAlignment="Stretch"
                     IsReadOnly="true"
                     Text="{Binding Title}"/>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4" HorizontalAlignment="Right">
            <Button Content="Ok" Click="OnButtonOkClicked" Padding="4" MinWidth="150"/>
        </StackPanel>
    </Grid>
</UserControl>
